<div id="lipscore-rating-wrapper">
    <div id="lipscore-rating"
        ls-product-name="{$name}"
        ls-brand="{$brand}"
        ls-product-id="{$id}"
        ls-gtin="{$id}"
        ls-product-url="{$url}"
    ></div>
</div>