<div class="card card-block" id="lipscore-ratings-and-form">

    <h2>{l s='Product reviews' mod='ko_lipscore'}</h2>

    <div id="lipscore-review-post"
        ls-product-name="{$name}"
        ls-brand="{$brand}"
        ls-product-id="{$id}"
        ls-gtin="{$id}"
        ls-product-url="{$url}"
    ></div>
    <br>
    <div id="lipscore-review-list"
        ls-product-name="{$name}"
        ls-brand="{$brand}"
        ls-product-id="{$id}"
        ls-gtin="{$id}"
        ls-product-url="{$url}"
    ></div>


</div>