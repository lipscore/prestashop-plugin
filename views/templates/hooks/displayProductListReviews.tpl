<div class="lipscore-rating-small-wrapper">
    <span class="lipscore-rating-small"
        ls-product-name="{$name}"
        ls-brand="{$brand}"
        ls-product-id="{$id}"
        ls-gtin="{$id}"
        ls-product-url="{$url}"
    ></span>
</div>
